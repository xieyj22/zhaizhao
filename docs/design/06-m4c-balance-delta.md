# M4c 地形前后平衡快照对比（Task A6）

- 日期：2026-09-18
- HEAD：`1d1280f`（worktree `worktree-m4c-terrain-visual`，A1-A5 地形全量：数据层+TerrainGen+resolver 三钩子+end_turn/builder 接线+AI 地形项）
- 工具：`src/playtest/balance_probe.gd`（4 seed 范围 × 3 性格 × 30 seed = 360 局；范围 base ∈ {100,500,1000,2000}，seed = base+i*13）
- 回归守护：全套 GUT **416/416 绿**（47 scripts / 9864 asserts / 54.6s，含旧 389 全量）——M0-M2 逐字节守护成立
- **结论：地形平衡 PASS**（2026-09-18 主会话裁决后定稿，依据 9ac7304 同语义对照，见 ⑤；stalled 高企为预存议题另案，见 ⑥）

## ① 谱系参考：4d63bec 调平后快照（软失败前语义——裁决后降级为脚注，不作对比基准）

commit `4d63bec`（tune(m4): 玩家侧生存性调平）commit message 记录：brain 17-33% / brute 7-27% / trick 13-23%，12 格全 non-0 non-100。本任务以 `git archive 4d63bec` 导出树**复跑探针核实**，数字逐格一致（复跑另坐实：当时 stalled 全 12 格 = 0）：

> **主会话裁决（2026-09-18，已记 ledger）**：该快照处于软失败落地前，dead/stalled 与现语义不可比（death% 77-93% vs 现 30-50%），作对比基准是错的——对比基准改用 `9ac7304`（同为软失败后语义，见 ③/⑤），本节降级为谱系脚注。

| range | persona | clear% | death% | draw% | stall% |
|------|---------|-------:|-------:|------:|-------:|
| 100 | brain | 17% | 83% | 0% | **0%** |
| 100 | brute | 10% | 90% | 0% | **0%** |
| 100 | trick | 23% | 77% | 0% | **0%** |
| 500 | brain | 20% | 80% | 0% | **0%** |
| 500 | brute | 10% | 90% | 0% | **0%** |
| 500 | trick | 23% | 77% | 0% | **0%** |
| 1000 | brain | 33% | 67% | 0% | **0%** |
| 1000 | brute | 27% | 73% | 0% | **0%** |
| 1000 | trick | 13% | 87% | 0% | **0%** |
| 2000 | brain | 23% | 77% | 0% | **0%** |
| 2000 | brute | 7% | 93% | 0% | **0%** |
| 2000 | trick | 17% | 83% | 0% | **0%** |

基线特征：stalled **全 12 格 = 0**；无边界格；探针内置 Step3 标准 PASS；确定性自检通过。

> 谱系脚注（不作对比基准）：更早 `5edba00` 快照为调平前 cleared 6.7% / dead 93.3%。

## ② 地形后实测（HEAD = 1d1280f）

| range | persona | clear% | death% | draw% | stall% |
|------|---------|-------:|-------:|------:|-------:|
| 100 | brain | 7% | 30% | 3% | 60% |
| 100 | brute | **0%** | 47% | 3% | 50% |
| 100 | trick | 10% | 37% | 7% | 47% |
| 500 | brain | 23% | 33% | 0% | 43% |
| 500 | brute | 10% | 47% | 3% | 40% |
| 500 | trick | 20% | 37% | 3% | 40% |
| 1000 | brain | 33% | 30% | 0% | 37% |
| 1000 | brute | 17% | 33% | 0% | 50% |
| 1000 | trick | 17% | 47% | 0% | 37% |
| 2000 | brain | 27% | 37% | 0% | 37% |
| 2000 | brute | 10% | 50% | 3% | 37% |
| 2000 | trick | 13% | 47% | 7% | 33% |

确定性自检：通过。探针内置 Step3 标准：FAIL（边界格 range=100 brute clear%=0%）。

## ③ 逐格 delta（谱系列 vs 4d63bec）+ 地形隔离对照（vs 9ac7304，**判定基准**）

为区分「地形造成」与「地形之前已存在」，本任务额外导出 `9ac7304`（地形 5 提交的前一个提交，即 M4b 叙事收口后，同为软失败后语义）复跑探针作隔离对照——裁决认定这才是 apples-to-apples 的正确对照：

| range | persona | 基线 clear% | 地形前 9ac7304 | 地形后 HEAD | Δ vs 基线 | Δ vs 9ac7304 |
|------|---------|------------:|---------------:|------------:|----------:|-------------:|
| 100 | brain | 17% | 7% | 7% | -10pp | **0pp** |
| 100 | brute | 10% | 0% | 0% | -10pp | **0pp** |
| 100 | trick | 23% | 10% | 10% | -13pp | **0pp** |
| 500 | brain | 20% | 23% | 23% | +3pp | **0pp** |
| 500 | brute | 10% | 10% | 10% | 0pp | **0pp** |
| 500 | trick | 23% | 20% | 20% | -3pp | **0pp** |
| 1000 | brain | 33% | 37% | 33% | 0pp | **-4pp** |
| 1000 | brute | 27% | 20% | 17% | -10pp | **-3pp** |
| 1000 | trick | 13% | 13% | 17% | +4pp | **+4pp** |
| 2000 | brain | 23% | 23% | 27% | +4pp | **+4pp** |
| 2000 | brute | 7% | 10% | 10% | +3pp | **0pp** |
| 2000 | trick | 17% | 13% | 13% | -4pp | **0pp** |

**地形净效应（9ac7304 → HEAD）每格 ≤4pp**（n=30 下二项噪声 σ≈8-9pp，1 局翻转=3.3pp），且 stalled 同为 33-60% 区间——地形对 outcome 分布的改变在噪声量级内，属预期波动。

## ④ stalled 检查

- 基线 4d63bec：全 12 格 stalled=0
- 地形前 9ac7304：全 12 格 stalled=33-57%
- 地形后 HEAD：全 12 格 stalled=33-60%（每格 10-18 局/30）
- → stalled 从 0 暴涨发生**在 4d63bec 与 9ac7304 之间的 34 个提交内**，非地形引入。

## ⑤ 判定（2026-09-18 主会话裁决后定稿）

**判定：地形平衡 PASS。**

**判定依据**——vs `9ac7304`（地形前最后提交，同为软失败后语义）的 apples-to-apples 对照：

- 地形净效应每格 clear% **≤4pp**：1000-brain -4pp / 1000-brute -3pp / 1000-trick +4pp / 2000-brain +4pp，其余 8 格 **0pp**（n=30 下 1 局翻转=3.3pp、σ≈8-9pp，纯噪声量级）；
- stalled 各格变化 ±3pp 内（33-60% ↔ 33-57%），无地形致 stall 迹象；
- 确定性自检通过；GUT 416/416 全绿。
- → 地形改变具体处境属预期波动，未破坏平衡。**TerrainGen 密度常量无罪，不需要调。**

**判定门触发的原始记录（vs 4d63bec，按 brief 原文，两条均触发）**：①clear% 较区间中点跌幅 >15pp——brain/100 实测 7% vs 中点 25% = -18pp，brute/100 实测 0% vs 中点 17% = -17pp（且为边界格）；②任一 stalled>0——全 12 格 33-60%（基线全 0）。裁决认定：该门所用基准是软失败前语义，dead/stalled 口径不可比，触发信号全部为语义迁移噪声（见 ⑥），不构成地形塌方。

**stalled 33-60% 定性：master@m4b 预存状况（软失败×MAX_NODES 语义迁移：普通节点败北不再 permadeath，残息续闯跑图至超限判 stalled），非 m4c 引入（9ac7304 已 33-57%），另案待用户裁决**——分析材料见 ⑥。

## ⑥ stalled 议题分析材料（另案待用户裁决）

塌方信号在 9ac7304（地形前）已完整存在：同样的边界格（brute/100=0%）、同样的 stalled 33-57%、death% 同为 30-50%。与 4d63bec 相比的结构性变化：

- **death% 77-93% → 30-50%，stalled 0 → 33-60%**——这正是「普通节点败北不再 permadeath、残息续闯」的签名：大量原本判 dead 的局改为继续跑图直至超 MAX_NODES 判 stalled。
- 嫌疑提交（4d63bec..9ac7304 中改结算流向的，按嫌疑排序）：
  - `65dc1ce` feat(m4): option B 软失败——普通节点败北残息续闯（boss 败仍 permadeath）
  - `8d3408f` fix(map): survive_loss 退回章首可换路
  - `e857435` fix(battle): MOVE 被占据格阻挡（战棋不可重合，AI 路径受限→战局拉长）
  - `9941971` fix(battle): 撤退回到进战前位置（可换打同层兄弟节点）

该议题的裁决选项（保留作分析材料）：
- A. 重定基线（接受软失败后的分布，改判定门为针对新语义的 stalled 上限/边界格标准）；
- B. 视 stalled 高企为软失败/MAX_NODES 交互 bug，先修 meta 层再重比；
- C. 其他。

## ⑦ 复现命令

```bash
G=E:/claude/Godot_v4.7-stable_win64_console.exe; P=<项目根>
$G --path $P --headless -s addons/gut/gut_cmdln.gd -gexit          # 416/416
$G --path $P --headless -s res://src/playtest/balance_probe.gd    # 360 局
# 基线复跑：git archive <sha> 导出裸树 → --headless --import → 同上跑探针
```
